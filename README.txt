Задание, PHP:
1. Задача может быть решена полностью или частично, и на разных уровнях
Есть модель представленная в виде дерева (например какой-то каталог), каждый элемент каталога может иметь потомков,
по сути связь модели с самой собой
- необходимо описать как хранить такие связи в БД
- реализовать метод для получения списка всех потомков в виде дерева
- реализовать метод для получения всех потомков в виде плоского списка
подумать о том как эффективно реализовать такие запросы
желательно сделать используя php фреймворк Laravel
Если  вы не знакомы с Laravel, то на чистом php, используя pdo.

Примечание: запрещается использовать сторонние пакеты для решения задачи хранения и извлечения дерева каталога.
Подразумевается вывод всех потомков в порядке вложенности, пример:
- уровень 1
- потомок 1.1
- потомок 1.2
- потомок 1.2.1
- уровень 2
- потомок 2.1
...
при этом дополнительно метод может обрабатывать параметр для указания родителя - и вывода всех его потомков,
если параметр не указан - то выводятся все записи (это условие не обязательно)

В начале я решил, что модель может быть представлена в виде структуры данных дерево-234, но потом понял,
что оно не дает правильного отображения, как приведено в примере. Тем не менее, для изучения языка PHP,
я решил попробовать реализовать его. Затем я попытался подключить базу данных PostgreSQL через Docker, так как считал,
что именно с базой данных нужно выполнить это задание. Однако у меня возникли проблемы,
и я так и не смог подключить ее к PhpStorm из контейнера. Поэтому я оставляю в репозитории то,
что мне удалось реализовать, а именно - дерево-234

Реализация структуры данных типа дерево-234:
Дерево-234 состоит из 4 модулей и 3 классов. DataItem - класс элемента узла, Node - класс узла дерева и
Tree234 - класс дерева-234.
Для взаимодействия с данной структурой данных необходимо запустить файл Tree234App, которая
предложит некоторый перечень действий с деревом-234

Запуск:
1. Убедитесь, что у вас установлена и настроена среда разработки PhpStorm для работы с PHP
(или любая другая среда разработки для работы с PHP).
2. Скачайте проект "Test_case_in_php" из репозитория на свой компьютер.
3. Откройте проект в PhpStorm.
4. В навигационной панели проекта найдите файл "Tree234App.php" и запустите его